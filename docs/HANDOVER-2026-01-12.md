# Session Handover — 2026-01-12

## What We Did This Session

### 1. Domain Connection (Completed)
- Connected `mitlivmed.dk` to Vercel via one.com DNS
- Preserved MX records for Google Workspace email
- MIT-14 and MIT-16 marked Done

### 2. OG Image Creation (Completed)
- Built `scripts/generate-og-image.mjs` using `@napi-rs/canvas`
- Downloaded brand fonts: Reddit Sans, Satoshi, Figtree
- Output: `public/og-image.png` (1x, 1200×630, 215KB) and `og-image2x.png` (2x)
- Design: Logo + wordmark + beta tag, hook text with orange underline on "efter", waves background, sticker CTA

### 3. MIT-28 Planning (Ready to Implement)
- Decided on `react-helmet-async` + static `index.html` fallbacks
- Plan file updated: `/Users/sorenfuhr/.claude/plans/snug-wibbling-puddle.md`
- Linear ticket updated with implementation details

---

## Next Task: MIT-28 Implementation

### Files to Create/Modify

1. **Install dependency**
   ```bash
   npm install react-helmet-async
   ```

2. **`src/main.tsx`** — Wrap app in `HelmetProvider`

3. **`src/components/SEO.tsx`** — Create reusable component:
   ```tsx
   interface SEOProps {
     title?: string
     description?: string
     canonical?: string
     noIndex?: boolean
   }
   ```

4. **`index.html`** — Update:
   - OG image: `https://mitlivmed.dk/og-image.png` (currently lovable.dev placeholder)
   - Add `og:url`, `og:site_name`, image dimensions
   - Add JSON-LD Organization + WebPage schemas

5. **`src/pages/*.tsx`** — Add `<SEO>` to each route:
   - Index: "MitLivMed – Livet efter diagnosen"
   - Privacy: "Privatlivspolitik | MitLivMed"
   - Terms: "Vilkår | MitLivMed"
   - Cookies: "Cookiepolitik | MitLivMed"
   - About: "Om Os | MitLivMed"

6. **`public/sitemap.xml`** — Create static sitemap

7. **`public/robots.txt`** — Add sitemap reference

### Verification
- Test browser tab titles change per route
- Test social previews: opengraph.xyz, Twitter validator, Facebook debugger

---

## Project Context

- **Repo:** `MitLivMed/mlw-prelaunch-website`
- **Tech:** Vite + React + TypeScript + Tailwind + shadcn/ui
- **Live:** https://mitlivmed.dk
- **Linear Project:** Pre-launch website

### Key Docs
- `docs/KICKOFF.md` — Dev kickoff, ticket status
- `docs/mitlivmed-meta-tags.md` — Meta tag reference (written for Next.js, adapt for Vite)
- `docs/MitLivMed-Brand-Book-v0.5.md` — Brand guidelines

### Deployment
```bash
git push origin main && git push personal main --force
```

---

## Open Tickets After MIT-28

| ID | Title | Notes |
|----|-------|-------|
| MIT-30 | Tally + HubSpot | Form integration |
| MIT-31 | Pre-launch testing | Lighthouse, a11y, cross-browser |
| MIT-41 | Section divider alternatives | Backlog |
